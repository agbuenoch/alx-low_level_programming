#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */
int main()
{
  printf("(A tortoise, having pretty good sense of a hare's");
  printf(" nature, challenges one to a race.)\n");
  
  return (0);
}
