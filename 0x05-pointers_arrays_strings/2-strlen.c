#include "main.h"
/**
 * _strlen - check the code
 *
 * @s: first pointer-to-character
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
int i;
int len;
for (i = 0; s[i] != '\0'; i++)
{
len = i + 1;
}
 return (len);
}
