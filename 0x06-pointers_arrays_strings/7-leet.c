#include "main.h"
/**
 * leet - substitutes character with numbers.
 *
 * @str: first pointer-to-character parameter.
 *
 * Return: pointer-to-character
*/
char *leet(char *str)
{
  int i;
  for (i = 0; str[i] != '\0'; i++)
    {
      str[i]
    }
  
  return str;
}
