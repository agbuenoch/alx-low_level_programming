#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @a: an array of integers
 * @n: the number of elements to swap
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
  reverse_array(1994);
  return (0);
}
