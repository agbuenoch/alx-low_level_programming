#include "main.h"
/**
 * _strpbrk - locate first occurrence in a string
 *
 * @s: first pointer-to-character
 * @accept: second pointer-to-character
 *
 * Return: pointer-to-character
*/
char *_strpbrk(char *s, char *accept)
{
return (s);
}
