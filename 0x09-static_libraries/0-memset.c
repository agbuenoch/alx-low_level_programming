#include "main.h"
/**
 * _memset - set memory with a constant value
 *
 * @s: first pointer-to-character
 * @b: character pointer
 * @n: integer parameter
 *
 * Return: pointer-to-character
*/
char *_memset(char *s, char b, unsigned int n)
{
return (s);
}
