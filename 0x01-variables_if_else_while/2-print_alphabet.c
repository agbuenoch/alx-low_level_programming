#include <stdio.h>
/**
 *main - entry point
 *
 *Return: always 0 (success)
 */
int main(void)
{
char alphabeth;
alphabet = 'a';
while(alphabet <= 'z')
{
putchar(alphabet);
alphabet++;
}
return (0);
}
