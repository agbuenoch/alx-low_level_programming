#include "main.h"
#include <stdio.h>
/**
 * _isdigit - check the code
 *
 * @c: first parameter.
 *
 * Return: Always 0.
 */
int _isdigit(int c)
{
  if (c >= 'a' && c <= 'Z')
{
return (0);
}
else
{
return (1);
}
}
