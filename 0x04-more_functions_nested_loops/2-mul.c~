#include "main.h"
/**
 * mul - multiplies two integer values
 *
 * @a: first parameter
 * @b: second parameter
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
int result;
result = a * b;
 return (result);
}
