/**
 * _isalpha - return 1 if argument is a letter, lower or uppercase..
 *
 * @c: first member
 *
 * Return: Always 0.
 */
int _isalpha(int c)
{
if (c >= 'A' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
