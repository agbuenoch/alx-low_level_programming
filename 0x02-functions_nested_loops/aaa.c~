#include <stdio.h>
/**
 * jack - check the code
 *
 * Return: Always 0.
 */
void jack(void){
  int hour;
  hour = 00;
  while (hour <=23)
    {
      int min;
      min = 00;
      while (min <= 59)
	{
	  printf("%d:%d\n", hour, min);
	  min++;
	}
      hour++;
    }
}

int main(void){
  jack();
  return (0);
}
